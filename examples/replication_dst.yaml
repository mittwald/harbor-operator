# Replication example with a destination registry
# Using a destination registry equals using the 'Push-based' option for 'Replication Mode'
# This pushes the specified resources from local to the remote registry
# Filters and triggers are optional
apiVersion: registries.mittwald.de/v1alpha1
kind: Replication
metadata:
  name: test-replication-dst
  namespace: harbor-operator
spec:
  id: 2
  name: test-replication-dst
  parentInstance:
    name: test-harbor
  deletion: false
  override: true
  enabled: true
  dest_registry: # you have to create the dest_registry via a registry custom resource first
    name: test-registry
#  filters:
#    - type: name
#      value: alpine
#    - type: tag
#      value: latest
#  trigger:
#    type: manual
#    trigger_settings:
#      cron: ""
