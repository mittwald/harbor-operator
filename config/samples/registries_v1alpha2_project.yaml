apiVersion: registries.mittwald.de/v1alpha2
kind: Project
metadata:
  name: repository-1
  namespace: harbor-operator
spec:
  memberRequests:
  - role: ProjectAdmin # one of "ProjectAdmin", "Developer", "Guest" or "Master"
    user:
     name: "harbor-user" # reference to a user object
  storageLimit: 10 # storage quota in GB
  name: repository-1
  parentInstance:
    name: test-harbor
#  metadata:
#    enableContentTrust:     false
#    autoScan:               false
#    severity:               "none"
#    reuseSysSVEWhitelist:   false
#    public:                 false
#    preventVul:             false